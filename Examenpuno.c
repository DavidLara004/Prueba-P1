#include <stdio.h>

int main() {
    int n;
    printf("Ingrese un numero entero positivo:");
    scanf("%d", &n);
    if (n>=0)
    {
        
    }
    else
    {
    printf("El numero ingresado no cumple con las condiciones.");
    }
    
    return 0;
}